<script type="text/javascript">
	$(document).ready(function () {

		CKEDITOR.plugins.addExternal( 'news', '{{ path('ckeditor_news_plugin') }}' );
		editor_{{ item.vars.id }} = CKEDITOR.replace('{{ item.vars.id }}', {{ item.vars.options | json_encode | raw }});


	});
</script>